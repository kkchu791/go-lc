### 21.2-6 No tail approach
```
UNION(x, y Nodes)

// find there set
sX = FIND-SET(x)
sY = FIND-SET(y)

// splice Sy into whever x is
x.next = sY.head
// how do i complete teh splice without sY's tail

old_sY_next = sY.head.next
sY.head.next = sX.head
sX.head.next = old_sY_next
sX.head = sY.head
```


```
1) Save rest of list y
    old_sY_next = sY.head.next

2) Insert sX's head right after sY.head
    sY.head.next = sX.head

3)connect sX's old head's next to the rest of list Y
    sX.head.next = old_sY_next

4) update sX.head to point to sY.head
    sX.head = sY.head
```


Iter 2

UNION-NO-TAIL(x, y):
    sX = FIND-SET(x)
    sY = FIND-SET(y)
    
    if sX == sY:
        return

    old_sX_next = sX.head.next
    old_sY_next = sY.head.next

    sX.head.next = old_sY_next
    sY.head.next = old_sX_next
    
    // Note: To fully connect into one chain without a tail pointer,
    // point the set object head to sX.head