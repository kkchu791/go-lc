### 21.2-3

aggregate proof of Theorem 21.1 amortized time bounds of O(1) for make-set & find-set O(lgn) for union

key insight - counting how many times a single elements set pointer can be updated

a sets size can double at most lgn times before reaching n.

set pointer of any individual el x is updated at most lgn times

amortized time complexity - method used in algo analysis to express the average time spent per operation

**amortized analysis**
aggregate proof - calculate an upper bound on total cost of an entire seq of n operations
accounting method- assigning fixed artificial charge for operations
potential method - uses mth to measure potential energy in DS

1. Count Pointer updates per element
    * pointer updated at most lgn times

2. sum up the total work
    * n el created by make-set
    * total pointer updates across all unions is at most nlogn
    * make and find always takes O(1) time
    * any seq of m operations containing n make-set calls takes o(m_nlgn) total time.

3. Divide to get amortized costs s
    Make-set cost = O(1)
    Find-Set cost = O(1)
    Union cost = O(logn) for one union

