### Theorem 21.1

Linked Lists of disjoint sets and the weighted union heuristic, a sequence of m Make-Set, Union, and Find set operations, n make-set operations,

Takes O(m+nlogn) time

### 21.1 Proof 

{a} {b}

n - 1 unions operation we perform over all.

Finding an upper bound

each time x's pointer was updated, x must have started in a smaller set

-> because the smaller set always get moved to the bigger set.

Total time spent updating object pointer over all union operations is O(nlogn)

Updating tail pointer takes only O(1) per union operator

Union total: O(nlogn)

Make-Set and Find Set O(1) but those are O(m of them)

Total time for the entire sequence is **O(m+nlogn)**